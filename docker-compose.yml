services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ai-guard-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped
